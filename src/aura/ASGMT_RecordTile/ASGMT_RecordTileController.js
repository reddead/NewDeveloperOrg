({
  doInit: function(component, event, helper) {
    helper.doInit(component);
  },

  editRecord: function(component, event, helper) {
    helper.editRecord(component);
  },

  editCriteriaSet: function(component, event, helper) {
    helper.editCriteriaSet(component);
  },

  confirmDeletion: function(component, event, helper) {
    helper.confirmDeletion(component, event);
  },

  deleteRecord: function(component, event, helper) {
    helper.deleteRecord(component, event);
  },

  fireASGMT_RecordTileEvt: function(component, event, helper) {
    helper.fireASGMT_RecordTileEvt(component, event);
  },

  getRelatedList: function(component, event, helper) {
    helper.getRelatedList(component, event);
  },

})
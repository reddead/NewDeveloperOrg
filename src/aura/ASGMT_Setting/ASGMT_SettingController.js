({
	doInit: function(component, event, helper) {
		helper.doInit(component);
	},

	pillButtonClicked: function(component, event, helper) {
		helper.pillButtonClicked(component, event);
	},

	deactivateAssignment: function(component, event, helper) {
		helper.deactivateAssignment(component, event);
	},

	addConfiguration: function(component, event, helper) {
		helper.addConfiguration(component,event);
	}

})